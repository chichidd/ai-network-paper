# Figure 1 v4 generation prompt

**Mode:** Built-in GPT image generation/editing.

**Edit target:** `post-training-federation-overview-v3.png`

**Style reference:** the user-provided three-stage audit diagram, used only for its varied small academic pictograms.

## Prompt

Use case: infographic-diagram

Asset type: full-width two-column academic security-paper workflow figure, new version of Figure 1.

Edit the v3 figure by adding a restrained but visibly more diverse set of small logo-like pictograms. Preserve the complete three-stage workflow, every box, all arrows, trust boundaries, colors, and every text label. Do not redesign the information architecture.

Add small distinct outlined icons beside or inside relevant nodes: heterogeneous frozen detector packages (decision tree, neural network, and rule/grid motifs); registry verification as a database with an inspection/check badge; deployment sites as building/server endpoints; residual composer as a compact network/mixer; secure aggregation as connected nodes converging into a sum/gear; coordinator as a server/operator console; local personalization as tuning sliders; held-out audit set as a database with a magnifier; paired audit as a balanced comparison/checklist; the decision gate as a judge/magnifier; Adopt Candidate with a green check; and Use Audited Incumbent with an amber return/anchor badge.

Use clean, publication-ready, vector-like flat illustration. Keep the pictograms colorful but restrained, with white interiors, crisp black/navy strokes, blue accents, and green/amber reserved for the two outcomes. Keep the exact wide aspect ratio and nearly identical geometry. Icons must use existing whitespace and must not obscure text or arrows. The figure must remain readable at two-column paper width.

Preserve all mathematical notation and existing labels verbatim. Preserve the coordinator/secure-aggregation boundary, the statement that per-example site data stay local, and the single paired-audit decision with two mutually exclusive outcomes. The icons may communicate heterogeneous packages and roles, but must not imply differential privacy, behavior certification, malicious-security protection, or a new guarantee.

Avoid brand logos, company marks, padlocks, shields, the copyright/privacy/accountability/provenance labels from the style reference, decorative characters, 3D effects, shadows, gradients, dense clip art, changed text, missing arrows, or a cartoon-poster appearance.
